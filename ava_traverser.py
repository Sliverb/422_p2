from imports import *
from sklearn.tree import DecisionTreeClassifier
from datasets import *

def contains(self, start):
	if(start.


h = multiclass.OAA(20, lambda: DecisionTreeClassifier(max_depth=1))
h.train(WineData.X, WineData.Y)

for i to range(17):
     tree=h.f[17][i]

i=18
for i to range(21):
     tree=h.f[i][17]


	
